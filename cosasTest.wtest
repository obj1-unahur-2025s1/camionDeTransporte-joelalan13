import cosas.*

describe "group of test for cosas" {

  method initialize() {
      residuosRadiactivos.peso(30)
      ladrillo.cantidad(10)
      embalaje.embala(residuosRadiactivos)
      contenedor.contiene(arenaAGranel)
      contenedor.contiene(bateriaAntiaerea)
      contenedor.contiene(ladrillo)
  }
  test "" {
    
  }
}